# Lambda 배포 가이드 (R2 연동 버전)

이 문서 하나만 순서대로 따라 하면 됩니다. `<...>` 부분만 본인 값으로 바꾸세요.

## 이번 변경 요약
- **로컬**: 하나도 안 바뀜. 기존처럼 `data/` 폴더에 parquet 수집·백테스트·매매 그대로.
- **Lambda**: 매매 전용이되 **데이터 수집은 직접 한다.** 시작할 때 R2에서 어제까지의 데이터를
  내려받고 → **오늘치만 증분 수집** → 패널 재빌드 → 매매 → 갱신분을 다시 R2로 업로드.
- **첫 실행**: Lambda가 처음부터 몇 년치를 받으면 15분 타임아웃에 걸리므로, **로컬에서 초기
  데이터를 R2에 한 번 넣어준다(3단계).** 그 뒤부터는 매일 오늘치만 붙이므로 빠름.
- **오래된 데이터**: `QUANT_RETENTION_DAYS` 를 주면 오래된 날짜별 런타임 파일을 자동 정리.

### 왜 코드 수정이 필요했나 (중요)
Lambda 파일시스템은 `/tmp` 를 빼면 **읽기전용**이라 기존처럼 `data/` 에 수집이 안 되고,
`/tmp` 는 실행이 끝나면 **싹 지워져서** 매일 처음부터 다시 수집하다 15분 타임아웃에 걸린다.
그래서 다음 접착 코드가 반드시 필요하다: ① 데이터 경로를 `/tmp` 로 돌리기 ② 시작 시
R2 → `/tmp` 다운로드 ③ 종료 시 `/tmp` → R2 업로드. 이건 **S3든 R2든 똑같이 필요**하며(원래
S3 코드가 있던 게 아니라 계획만 있었음), R2는 접속주소·키만 다르다. 코드가 **S3 호환 API**를
쓰기 때문에 Oracle Cloud·Backblaze 등으로도 환경변수 4개만 바꾸면 그대로 붙는다.

### 바뀐 파일
- `src/config/paths.py` — 수집 저장경로가 `QUANT_DATA_DIR` 을 따르게(로컬은 그대로 `data`).
- `src/live/state.py` — 동적설정 경로도 `QUANT_DATA_DIR` 을 따르게.
- `src/live/remote_store.py` — **신규.** R2/S3 동기화 + 초기 주입 CLI + 보존기간 정리.
- `lambda_handler.py` — 시작 시 R2 다운로드·strategy 시드, 매매, 종료 시 R2 업로드.
- `requirements.txt` — `boto3` 추가.
- `.env` — R2 접속정보 항목 추가.

---

## 1단계 — Cloudflare R2 버킷 + 키 만들기
1. Cloudflare 대시보드 → **R2** → **Create bucket** → 이름 `quant-live` (원하는 이름).
2. R2 첫 화면의 **Account ID** 복사 → endpoint 주소:
   `https://<계정ID>.r2.cloudflarestorage.com`
3. **Manage R2 API Tokens** → **Create API token** → 권한 **Object Read & Write** →
   **Access Key ID** 와 **Secret Access Key** 복사(한 번만 보여줌).

---

## 2단계 — 로컬 `.env` 채우기
`Quant/.env` 아래 부분을 실제 값으로 채우고 `REMOTE_STORE` 를 `1`로:

```
REMOTE_STORE="1"
S3_ENDPOINT_URL="https://<계정ID>.r2.cloudflarestorage.com"
S3_BUCKET="quant-live"
S3_ACCESS_KEY_ID="<Access Key ID>"
S3_SECRET_ACCESS_KEY="<Secret Access Key>"
S3_REGION="auto"
```

---

## 3단계 — 초기 데이터 R2에 주입 (로컬에서 1회)
Lambda가 첫날부터 몇 년치를 받지 않도록, **매매에 필요한 스코프만**(채택 알파가 쓰는
데이터셋 × 현재 top-100 심볼 + 패널·유니버스, 약 1.6GB) 로컬에서 올립니다.

```bash
cd C:\Users\moong\Desktop\Quant
python -m src.live.remote_store push      # 스코프 원본+패널+유니버스 업로드 (수 분 소요)
python -m src.live.remote_store list      # 올라간 개수 확인
```

> 전체 7.7GB가 아니라 라이브에 필요한 부분만 올라갑니다. 이후엔 Lambda가 매일 오늘치만
> 이어붙이므로 이 명령을 다시 돌릴 필요는 없습니다(원하면 언제든 다시 올려 덮어쓸 수 있음).

---

## 4단계 — 도커 이미지 빌드 & ECR 푸시
먼저 정할 값: `<AWS계정ID>`(12자리), `<리전>`(예 `ap-northeast-2`), `<ECR저장소>`(예 `quant-live`).

```bash
cd C:\Users\moong\Desktop\Quant

# (최초 1회) ECR 저장소 생성
aws ecr create-repository --repository-name <ECR저장소> --region <리전>

# ECR 로그인
aws ecr get-login-password --region <리전> | docker login --username AWS --password-stdin <AWS계정ID>.dkr.ecr.<리전>.amazonaws.com

# 빌드 (Lambda는 amd64만 지원 — 플랫폼 명시)
docker build --platform linux/amd64 -t <ECR저장소>:latest .

# 태그 & 푸시
docker tag <ECR저장소>:latest <AWS계정ID>.dkr.ecr.<리전>.amazonaws.com/<ECR저장소>:latest
docker push <AWS계정ID>.dkr.ecr.<리전>.amazonaws.com/<ECR저장소>:latest
```

---

## 5단계 — Lambda 리소스 & 환경변수 설정
Lambda는 `/tmp` 기본이 512MB인데 원본 데이터(약 1.6GB)를 내려받아야 하므로 **임시저장소를
키우고**, 수집·재빌드 시간을 위해 **타임아웃·메모리도 넉넉히** 잡습니다.

```bash
aws lambda update-function-configuration \
  --function-name <Lambda함수명> \
  --region <리전> \
  --timeout 900 \
  --memory-size 2048 \
  --ephemeral-storage '{"Size": 4096}' \
  --environment "Variables={
    QUANT_DATA_DIR=/tmp/quant-data,
    REMOTE_STORE=1,
    S3_ENDPOINT_URL=https://<계정ID>.r2.cloudflarestorage.com,
    S3_BUCKET=quant-live,
    S3_ACCESS_KEY_ID=<Access Key ID>,
    S3_SECRET_ACCESS_KEY=<Secret Access Key>,
    S3_REGION=auto,
    TRADING_MODE=testnet,
    BINANCE_API_KEY=<...>,
    BINANCE_SECRET_KEY=<...>,
    TESTNET_API_KEY=<...>,
    TESTNET_SECRET_KEY=<...>,
    TELEGRAM_BOT_TOKEN=<...>,
    TELEGRAM_CHAT_ID=<...>,
    TELEGRAM_ALLOWED_CHAT_ID=<...>,
    QUANT_RETENTION_DAYS=90
  }"
```

> - 실계좌 준비가 되면 `TRADING_MODE=live` 로만 바꾸면 됩니다(그 전엔 `testnet` 권장).
> - 처음 이미지로 함수를 새로 만드는 경우엔 `aws lambda create-function --package-type Image
>   --code ImageUri=... --role <실행역할ARN>` 로 만든 뒤 위 설정을 적용하세요.

---

## 6단계 — 코드 갱신 (앞으로 매번 이 명령)
코드를 고쳐 재배포할 땐 4단계(빌드/푸시) 후 이 한 줄:

```bash
aws lambda update-function-code \
  --function-name <Lambda함수명> \
  --region <리전> \
  --image-uri <AWS계정ID>.dkr.ecr.<리전>.amazonaws.com/<ECR저장소>:latest
```

## 7단계 — 동작 확인
```bash
aws lambda invoke --function-name <Lambda함수명> --region <리전> out.json && type out.json
```
`{"message":"success", ...}` 가 나오면 정상. 텔레그램으로도 결과가 옵니다.
CloudWatch 로그에서 "R2 다운로드 완료 N개", "R2 업로드 완료 N개" 가 찍히는지 보면 동기화가
잘 되는지 알 수 있습니다.

---

## 자주 막히는 지점
- **첫 실행이 `config.json 없음`/패널 에러** → 3단계 `push` 안 했거나 R2 키/버킷명이 Lambda
  환경변수와 불일치. `python -m src.live.remote_store list` 로 R2에 파일이 있는지부터 확인.
- **`No space left on device` / `/tmp` 오류** → 5단계 `--ephemeral-storage` 를 안 키운 경우.
- **첫 실행이 타임아웃** → 3단계 초기 주입을 안 해서 Lambda가 전체 백필을 시도한 것.
  로컬 push 를 먼저 하세요.
- **`docker push` 권한 오류** → ECR 로그인 명령 재실행(로그인은 만료됨).
- **`exec format error`** → 빌드 시 `--platform linux/amd64` 누락.
- **오라클/백블레이즈로 교체** → `S3_ENDPOINT_URL`, `S3_ACCESS_KEY_ID`,
  `S3_SECRET_ACCESS_KEY`, `S3_REGION` 4개만 교체. 코드 변경 없음.
