# Quant 프로그램 설명서

> 이 문서는 이 프로그램을 처음 보는 사람도 이해할 수 있게, 전문용어를 최대한 쉬운 말로 풀어서 설명합니다.
> 알파(전략) 이름 같은 고유명사는 영어 그대로 두되, 나머지는 한국어로 풀었습니다.

---

## 1. 이 프로그램은 한마디로 무엇인가

**여러 개의 "돈 버는 규칙"을 컴퓨터에게 시켜, 비트코인 선물(정확히는 바이낸스 USDT 무기한 선물) 시장에서 자동으로 사고팔게 하는 프로그램**입니다.

핵심 아이디어는 이렇습니다.

- 시장에는 "이런 코인은 앞으로 오를 확률이 조금 높더라", "저런 코인은 내릴 확률이 조금 높더라" 하는 **작은 규칙(패턴)** 들이 있습니다.
- 이런 규칙 하나하나를 이 프로그램에서는 **알파(alpha)** 라고 부릅니다.
- 알파 하나만 믿으면 위험하니, **서로 성격이 다른 알파 여러 개를 섞어서** 더 안정적인 하나의 팀(포트폴리오)을 만듭니다.
- 그 팀이 "오늘은 이 코인을 이만큼 사고, 저 코인을 이만큼 팔아라"라고 정해주면, 프로그램이 **안전장치(리스크 관리)** 를 한 번 더 씌운 뒤 실제 주문을 냅니다.

즉 이 프로그램은 크게 네 가지 일을 합니다.

1. **데이터 수집** — 코인 시장의 가격·거래량·펀딩 등 과거/현재 데이터를 모읍니다.
2. **알파 연구(백테스트)** — 규칙(알파)이 과거에 실제로 통했는지 시험합니다.
3. **포트폴리오 구성** — 통과한 알파들을 골라 섞고, 위험을 통제합니다.
4. **실매매/가상매매(라이브)** — 매일 오늘의 목표 비중을 계산해 주문을 내고, 결과를 텔레그램으로 보고합니다.

> **비유:** 여러 명의 애널리스트(알파)가 각자 "제 생각엔 이렇게 투자해야 합니다" 하고 의견을 냅니다.
> 매니저(combine)가 그중 실력 좋고 서로 안 겹치는 사람만 뽑아 의견을 종합하고,
> 리스크 담당자(risk)가 "너무 한쪽에 몰렸다", "요즘 손실이 크니 규모를 줄여라" 하고 브레이크를 밟은 뒤,
> 트레이더(live)가 실제 주문을 냅니다.

---

## 2. 전체 흐름 한눈에 보기

```
[1] 데이터 수집              [2] 알파 연구            [3] 포트폴리오          [4] 실매매
    collector/       ->        backtest/       ->      portfolio/     ->      live/
 유니버스 선정             알파 수식 → 신호            알파 선택·비중          오늘 목표비중
 klines/metrics/          → 백테스트 손익            리스크 안전장치          → 주문 → 원장
 funding/premium          → 검증(진짜인가?)         (combine + risk)         → 텔레그램 보고
```

이 문서의 나머지는 이 흐름을 따라가며, 각 단계에서 나오는 개념과 실제로 쓰는 방식, 그리고 각 모듈(코드 폴더)의 역할을 설명합니다.

---

## 3. 어떤 데이터를 수집하나

### 3-1. 대상 코인 고르기 — "유니버스(universe)"

세상의 모든 코인을 다 거래할 수는 없습니다. 그래서 **거래할 후보 코인 목록**을 먼저 정하는데, 이걸 **유니버스**라고 부릅니다.

이 프로그램의 유니버스 규칙(`universe_rules.json`)은 이렇습니다.

- **바이낸스 USDT 무기한 선물**만 대상 (만기 있는 계약, 코인 담보 계약 제외).
- **최근 30일 거래대금의 중앙값**이 큰 순서로 줄 세워 **상위 100개**를 편입.
- 한 번 들어온 코인은 순위가 **130위 밖으로 밀려날 때까지는 유지**(들락날락 방지 — 이걸 "이력밴드/hysteresis"라 합니다).
- 상장한 지 **30일 미만**인 코인은 제외.
- **매달 한 번**씩 목록을 새로 정함(월간 리밸런싱).

**중요 — 생존편향 방지:** 과거를 시험할 때 "지금 살아남은 코인" 기준으로 보면 결과가 뻥튀기됩니다(망한 코인이 빠지니까). 그래서 `universe_builder`가 **과거 매달 그 시점에 실제로 유니버스에 있던 코인 목록**을 하나하나 복원해서 저장해 둡니다. 2024년 3월을 시험할 땐 2024년 3월에 실제로 있던 코인만 씁니다.

### 3-2. 실제로 모으는 데이터 5종

수집기(`full_collector`)는 바이낸스의 공개 데이터 저장소(아카이브)와 실시간 API에서 다음을 모아 코인별 파일로 저장합니다.

| 데이터셋 | 뭘 담나 | 여기서 뽑아내는 값(예) |
|---|---|---|
| **klines** | 기본 가격 캔들(시가/고가/저가/종가/거래량) | close, open, high, low, volume, 거래대금, 체결건수, 시장가매수비중 |
| **metrics** | 포지션·심리 지표 | 미결제약정(OI), 롱숏 비율(계정 수/포지션 규모/체결 기준) |
| **premiumIndexKlines** | 선물이 지수 대비 얼마나 비싼지(프리미엄) | premium |
| **fundingRate** | 펀딩비(8시간마다 롱↔숏이 주고받는 수수료) | funding_rate |
| **bookDepth** | 호가창 깊이(사자/팔자 주문 두께) | 오더북 불균형, 호가 두께 |

> 기본값은 앞의 **4종(klines, premiumIndexKlines, metrics, fundingRate)** 만 모읍니다. bookDepth는 2023년부터의 데이터만 있어 선택적으로 모읍니다.

### 3-3. Klines가 뭔가

**Klines = 캔들 차트의 "봉" 데이터**입니다. K는 캔들(candle)의 일본어 발음에서 왔습니다.

일정 시간(예: 1시간, 하루) 동안 코인 가격이 어떻게 움직였는지를 숫자 몇 개로 요약한 것입니다. 한 개의 봉(캔들)은 보통 이렇게 생겼습니다.

- **시가(open)**: 그 시간 시작할 때 가격
- **고가(high)**: 그 시간 중 가장 높았던 가격
- **저가(low)**: 그 시간 중 가장 낮았던 가격
- **종가(close)**: 그 시간 끝날 때 가격
- **거래량(volume)**: 그 시간 동안 사고판 양

이 프로그램에서 가장 많이 쓰는 값은 **종가(close)** 입니다. "어제 종가 대비 오늘 종가가 몇 % 올랐나"로 수익률을 계산하기 때문입니다. 그 밖에 거래대금(quote_volume), 체결건수(trades), 시장가 매수 체결 비중(taker_buy_volume) 등도 알파 재료로 씁니다.

### 3-4. Metrics가 뭔가

**Metrics = 가격이 아니라 "시장 참가자들이 어떻게 포지션을 잡고 있는지"를 보여주는 지표 묶음**입니다. 바이낸스가 별도로 제공합니다. 대표적으로:

- **미결제약정(Open Interest, OI)**: 아직 청산 안 된 선물 계약이 얼마나 쌓여 있나 = 시장에 걸린 판돈의 크기.
- **롱숏 비율**: 사람들이 롱(오른다에 베팅)과 숏(내린다에 베팅) 중 어디에 몰려 있나. 세 가지 버전이 있습니다.
  - **계정 수 기준(global_ls_accounts)**: 일반 개미들이 어디로 쏠렸나 (사람 머릿수).
  - **상위 트레이더 포지션 규모 기준(toptrader_ls_positions)**: 큰손들이 돈으로 어디에 베팅했나.
  - **상위 트레이더 계정 수 기준(toptrader_ls_accounts)**: 큰손들이 머릿수로는 어디에 쏠렸나.
- **시장가 매수/매도 비율(taker_ls_ratio)**: 공격적으로 즉시 체결한 주문이 매수 쪽인가 매도 쪽인가.

이런 "포지셔닝" 데이터는 가격만 보는 알파와 성격이 달라서, 서로 안 겹치는(독립적인) 알파를 만드는 재료로 귀합니다.

### 3-5. 펀딩비(funding_rate) — 알아두면 중요

무기한 선물은 만기가 없어서, 선물 가격이 현물 가격에서 너무 벌어지지 않게 잡아주는 장치가 필요합니다. 그게 **펀딩비**입니다. **8시간마다** 롱과 숏 중 한쪽이 다른 쪽에게 수수료를 냅니다.

- 펀딩이 **양수**면: 롱이 숏에게 돈을 냅니다(= 롱 쏠림, 과열 신호).
- 펀딩이 **음수**면: 숏이 롱에게 돈을 냅니다.

이 프로그램은 펀딩을 두 가지로 씁니다.
1. **알파 재료**로: "펀딩 높은 코인은 과열됐으니 숏" 같은 규칙(funding_carry 계열).
2. **비용 계산**으로: 우리가 실제로 포지션을 들고 있으면 8시간마다 펀딩을 내거나 받으므로, 손익에 정확히 반영합니다.

---

## 4. 꼭 알아야 할 핵심 개념 사전

여기서부터는 이 프로그램 곳곳에 나오는 용어를 쉬운 말로 풀어 설명합니다.

### 4-1. 딜레이(delay) — 미래를 훔쳐보지 않기

가장 중요한 원칙입니다. **"오늘 종가를 보고 오늘 즉시 거래했다"고 계산하면 반칙**입니다. 현실에서는 오늘 종가가 확정되는 순간엔 이미 거래할 수 없기 때문입니다. 그렇게 계산하면 백테스트 성적은 좋아 보여도 실전에서는 재현이 안 됩니다. 이걸 **미래참조(look-ahead)** 라고 하고, 퀀트에서 가장 흔한 자기기만입니다.

그래서 이 프로그램은 **delay(지연)** 를 반드시 **1 이상**으로 강제합니다.

- 오늘 신호로 정한 비중은 **다음 봉(하루 뒤 등)에 가서야 거래**됩니다.
- 코드로는 `positions_t = weights_{t-delay}` — 즉 오늘 실제로 들고 있는 포지션은 delay일 전에 정한 것입니다.

이 한 줄 덕분에, 오늘의 손익 계산에 쓰는 모든 값이 "오늘 이전 정보"만 사용하게 되어 미래참조가 원천 차단됩니다.

### 4-2. 롱/숏, 그리고 롱숏 비율

- **롱(long)**: 오른다에 베팅(사두기). 비중이 양수(+).
- **숏(short)**: 내린다에 베팅(빌려서 팔기). 비중이 음수(−).
- **롱숏 비율 / 순노출(net exposure)**: 전체 포지션에서 롱과 숏의 균형. `순노출 = Σ(모든 비중)`.
  - 순노출 ≈ 0 이면 **시장중립**(롱과 숏이 비슷해서 시장 전체가 오르든 내리든 큰 영향 안 받음).
  - 순노출이 +쪽이면 전체적으로 롱에 기울어 있다는 뜻.

이 프로그램은 기본적으로 **시장중립**을 지향합니다. 대부분의 알파를 "그날 코인 평균을 빼서"(demean) 롱과 숏이 균형을 이루게 만들기 때문입니다. 다만 최근에는 신호가 정말 확신할 때 방향성을 조금 허용하는 규칙(뒤의 directional)도 도입했습니다.

### 4-3. 샤프 지수(Sharpe ratio) — 위험 대비 수익

전략이 얼마나 "꾸준히" 버는지를 재는 대표 점수입니다. 단순히 많이 버는 게 아니라, **변동성(출렁임) 대비** 얼마나 버는지를 봅니다.

**수식 (이 프로그램의 실제 계산):**

```
샤프 = (일별 손익의 평균 / 일별 손익의 표준편차) × √(연간 거래일수)
```

- **평균**: 하루 평균 얼마 벌었나.
- **표준편차**: 그 손익이 얼마나 들쭉날쭉했나(위험).
- **√(연간 거래일수)**: 하루치 점수를 1년 기준으로 환산하는 보정. 코인은 **365일** 쉬지 않고 거래되므로 주식(252)이 아니라 **365**를 씁니다.

직관적으로 **샤프가 높을수록 = 적게 출렁이면서 꾸준히 번다** = 좋은 전략입니다. 참고로 이 프로젝트는 리스크 스택 개선으로 샤프를 약 0.79에서 1.02까지 끌어올린 이력이 있습니다.

이 프로그램은 샤프를 세 가지 버전으로 계산해 "지금도 유효한지"를 봅니다.
- **sharpe**: 전체 기간 샤프.
- **sharpe_recent**: 최근 90일만의 샤프(요즘도 되나?).
- **sharpe_hl(반감기 가중)**: 최근을 더 무겁게 치는 샤프. 90일마다 옛날 성적의 가중치가 절반으로 줄어듭니다. 최근 성향을 부드럽게 반영하는 데 씁니다.

### 4-4. 그 밖의 성과 지표

- **ann_return(연환산 수익)**: 1단위 자본으로 1년 굴렸을 때 기대 수익(비용 다 뺀 뒤).
- **MDD(최대낙폭, Max Drawdown)**: 자본 곡선이 고점에서 저점까지 최대 얼마나 깎였나. **작을수록 안전**. "한때 −15%까지 물렸다" 같은 최악의 순간을 재는 값입니다.
- **turnover(회전율)**: 하루에 포트폴리오의 몇 %를 갈아엎나. **높을수록 수수료를 많이 냅니다.** 그래서 낮을수록 유리.
- **vol(변동성)**: 손익이 연 기준으로 얼마나 출렁이는지. 리스크 장치가 이걸 목표치(예: 연 15%)로 맞춥니다.
- **IC(정보계수, Information Coefficient)**: "우리가 높게 점수 준 코인이 실제로 더 올랐나"의 상관관계. 순위끼리 비교(스피어만)하며, 1·5·10·20일 뒤 수익을 각각 보고 평균 냅니다. IC가 양수이고 클수록 신호에 진짜 예측력이 있다는 뜻입니다.
- **fitness(적합도)**: 알파를 한 줄로 줄 세우기 위한 종합 점수. 아래 수식입니다.

```
fitness = 샤프 × √( |연환산수익| / max(회전율, 0.125) )
```

  뜻: **적게 사고팔면서(낮은 회전율) 높은 샤프를 버는 알파일수록 높은 점수**. 거의 거래 안 하는 알파가 무한대 점수를 받지 않게 회전율 하한(0.125)을 둡니다.

### 4-5. 알파란 무엇인가

**알파 = "이런 코인을 사고 저런 코인을 팔면 돈을 번다"는 규칙 하나**입니다. 이 프로그램에서 알파 하나는 **JSON 파일 하나**(`data/strategy/alphas/이름.json`)로 표현됩니다.

알파 파일의 핵심 항목:

- **name**: 이름(파일명과 같아야 함).
- **expression(수식)**: 신호를 만드는 계산식. 예: `rank(neg(funding_rate))`.
- **neutralization(중립화)**: 시장 방향성을 어떻게 제거할지. `none`(안 함), `market`(그날 평균 빼기=시장중립), `partial`(일부만), `turnover_rank`(거래대금 그룹 안에서만 비교).
- **decay(감쇠)**: 신호를 며칠에 걸쳐 부드럽게 펼지. 회전율을 낮추는 역할.
- **delay(지연)**: 위에서 설명한 미래참조 방지용. 반드시 1 이상.
- **bar(판단 주기)**: 얼마나 자주 다시 판단하나(1d=하루, 8h=8시간 등).
- **description**: 이 알파가 왜 돈을 벌 거라 믿는지 사람이 적어둔 가설.

**수식은 어떻게 신호가 되나?** 수식은 코인별·날짜별 표(패널) 위에서 계산됩니다. 예를 들어 `rank(neg(funding_rate))`는:
1. `funding_rate`: 각 코인의 펀딩비를 가져온다.
2. `neg`: 부호를 뒤집는다(펀딩 높은 코인이 낮은 점수가 되도록).
3. `rank`: 그날 코인들 사이 순위를 매긴다.
→ 결과: **펀딩 낮은 코인은 높은 점수(롱), 펀딩 높은 코인은 낮은 점수(숏)**.

수식에 쓰는 대표 연산자(operators):

| 연산자 | 뜻 |
|---|---|
| `rank(x)` | 그날 코인들 사이 순위를 (0,1]로 |
| `zscore(x)` | (값 − 평균) / 표준편차 (표준화) |
| `neg(x)` | 부호 뒤집기 |
| `scale(x)` | 그날 절대값 합이 1이 되게 정규화 |
| `demean(x)` | 그날 평균 빼기(시장중립) |
| `ts_mean(x, n)` | 최근 n일 평균 |
| `ts_rank(x, n)` | 오늘 값이 최근 n일 중 몇 등인지 |
| `ts_delta(x, n)` | n일 전 대비 변화량 |
| `returns(close, n)` | n일 수익률 |
| `div/mul/sub/add` | 나누기/곱하기/빼기/더하기 |

### 4-6. 지금 쓰고 있는 알파들 (한국어 요약)

현재 `data/strategy/alphas/`에 있는 알파들을 계열(family)별로 묶어 쉽게 풀면 다음과 같습니다. **이름은 영어 그대로, 설명은 한국어**입니다.

**펀딩·프리미엄 계열 (carry)** — 과열/쏠림을 되돌림에 베팅
- `funding_carry`, `funding_carry_7d`, `funding_carry_30d`: 펀딩(=롱 쏠림 비용)이 높은 코인은 숏, 낮은 코인은 롱. 7일/30일은 평균을 내 회전율을 낮춘 버전.
- `funding_carry_stable`: 꾸준히 높은 펀딩만 숏하고 널뛰는 펀딩은 무시(수준÷변동성).
- `funding_accel`: 펀딩이 평소보다 급등 중(3일>14일 평균)인 코인을 숏 = 쏠림이 새로 몰리는 중.
- `funding_carry_signed`: funding_carry_7d의 방향성 버전(뒤의 directional 규칙 대상).
- `premium_fade`: 선물이 지수 대비 프리미엄으로 마감한 코인을 숏(과열 되돌림).
- `spot_basis_fade`: 선물가가 실제 현물가 대비 비싼 코인을 숏(같은 논리, 현물가 직접 비교).

**모멘텀·가격위치 계열 (momentum)** — 오르는 추세는 이어짐에 베팅
- `range_high_60d`, `range_high_120d`: 최근 60/120일 가격 범위의 상단(신고가 근처)에 붙은 코인을 롱.
- `clean_momentum`: 펀딩이 안 낀(레버리지 추격이 없는) 코인의 20일 모멘텀만 롱.
- `mom60_turnover_neutral`: 60일 모멘텀을 거래대금 그룹 안에서만 비교(볼륨 편향 제거).
- `idio_momentum_20d`: 시장 평균을 뺀 초과 수익률의 20일 추세(개별 종목 고유 모멘텀).

**포지셔닝 계열 (positioning)** — 사람들의 쏠림을 이용
- `crowd_ls_fade`: 개미 계정이 한쪽으로 쏠리면 반대로 베팅(컨트래리언).
- `smart_dumb_divergence`: 큰손 포지션과 개미 포지션의 방향 차이를 따라감(스마트머니 추종).
- `toptrader_conviction_momentum`: 큰손이 5일간 확신을 늘리는 방향을 추세추종.
- `whale_concentration_fade`: 소수의 대형 계정이 몰빵 중인 얇은 쏠림을 페이드.

**유동성 계열 (liquidity)** — 거래가 얇은 코인의 프리미엄
- `amihud_illiq`: 거래대금 대비 가격 충격이 큰(비유동적) 코인은 유동성 프리미엄을 요구받음 → 롱.
- `book_liquidity_premium`: 호가창이 얇은 코인일수록 유동성 프리미엄(오더북 데이터 사용).
- `illiq_crowd_fade`: 비유동적이면서 동시에 쏠린 코인을 특정(두 계열의 곱).

**오더북·플로우 계열**
- `book_imbalance_momentum`: 호가창 매수/매도 불균형이 양수면 단기 상승 압력 → 롱.
- `taker_flow_follow`: 공격적 시장가 매수 비중이 높은 코인을 롱(주문흐름 모멘텀).

**단기 역추세 (reversal)**
- `reversal_5d`: 최근 5일 많이 오른 코인 숏, 많이 내린 코인 롱(단기 평균회귀).

> 각 알파 파일의 `description`에는 "왜 이게 돈을 벌 것이라 믿는지"와 "무엇을 확인해야 하는지"가 상세히 적혀 있습니다. 이 프로젝트의 방식은 **아무 숫자나 갖다 붙이지 않고, 경제적 이유(가설)가 있는 알파만** 만드는 것입니다.

---

## 5. 백테스트 — 알파가 진짜인지 시험하기

알파를 만들었다고 바로 믿으면 안 됩니다. **과거 데이터에 넣어 돌려보고(백테스트)**, 그게 우연이 아닌지 검증합니다.

### 5-1. 손익은 어떻게 계산되나 (엔진)

알파 엔진(`engine.py`)이 알파 하나를 손익으로 바꾸는 과정입니다.

```
수식 → 원점수 → 중립화(시장방향 제거) → scale(L1=1, 북 크기 고정)
     → decay(선택, 회전율 완화) → delay(오늘 신호는 내일 거래) → 손익
```

손익(정확한 회계)은 다음과 같이 계산됩니다.

```
그날수익률   = 오늘종가 / 어제종가 − 1
포지션       = delay일 전에 정한 비중
총손익(gross) = Σ(포지션 × 그날수익률)
회전율       = Σ|오늘포지션 − 어제포지션|
거래비용     = 회전율 × (테이커수수료 0.04% + 슬리피지 0.05%)
펀딩손익     = −Σ(포지션 × 펀딩)   ← 롱인데 펀딩 양수면 지불
순손익(net)  = 총손익 − 거래비용 + 펀딩손익
```

여기서 두 가지가 중요합니다.
- **모든 성과 지표는 "순손익(net)"에서 계산**됩니다. 즉 수수료·슬리피지·펀딩을 이미 다 뺀 진짜 손익입니다.
- **펀딩은 8시간마다 정확히 정산**됩니다(하루 3번). 대충 하루치로 합치지 않고, 그 순간 실제로 들고 있던 포지션에만 부과합니다.

### 5-2. 이게 우연은 아닌지 검증 (validation)

알파가 과거에 돈을 벌었어도 그게 운일 수 있습니다. 그래서 세 가지 정직성 검사를 합니다.

1. **OOS 분리(In-Sample / Out-of-Sample)**: 기간을 과거(학습)와 미래(시험)로 나눠, "본 적 없는" 미래 구간에서도 샤프가 살아있는지 봅니다.
2. **워크포워드(walk-forward)**: 기간을 여러 조각으로 나눠, 한 구간이 아니라 **대부분의 구간에서** 양수였는지 봅니다.
3. **블록 순열검정(permutation test)**: 손익을 며칠씩 묶어 순서를 수천 번 무작위로 섞어봅니다. 진짜 신호라면 실제 성적이 "섞은 것들"보다 확실히 좋아야 합니다(p값 < 0.05 = 유의).

특히 **워크포워드**는 이 프로젝트에서 가장 엄격한 검증입니다. 단순히 알파 하나가 아니라, **"알파를 고르고 비중을 주고 리스크를 씌우는 전 과정"을 매 시점마다 그때까지의 데이터만으로 다시 실행**해서, 미래참조가 0인 상태로 진짜 실전과 똑같이 재현합니다(`walkforward --config`).

### 5-3. 알파끼리 겹치는지 (상관 / 독립 그룹)

알파 두 개의 손익이 거의 똑같이 움직이면(상관 높으면), 둘을 같이 넣어도 분산 효과가 없습니다. 사실상 같은 알파니까요. 그래서 `backtest-corr` 명령으로 **손익 상관행렬**을 구하고, |상관|이 0.5 이상인 알파들을 한 덩어리로 묶어 **"진짜 서로 다른 베팅이 몇 개인지(독립 그룹 수)"** 를 셉니다. 포트폴리오의 진짜 힘은 알파 개수가 아니라 **독립적인 베팅의 개수**에서 나오기 때문입니다.

---

## 6. 포트폴리오 — 알파 선택·비중·퇴출 로직

통과한 알파들을 하나의 팀으로 묶는 단계입니다. 세 가지 결정을 합니다: **누구를 뽑나(선택)**, **얼마씩 주나(비중)**, **언제 빼나(퇴출)**.

### 6-1. 선택(selection) — 누구를 뽑나

`config.json`의 `selection` 설정에 따라 후보 알파를 거릅니다. 기본 방식은 **low_correlation(낮은 상관 우선)** 이고, fitness가 높은 순서로 훑으면서 다음 관문을 통과한 알파만 뽑습니다.

- **품질 관문(min_fitness)**: fitness가 기준 미만이면 탈락. "상관만 낮은 노이즈"가 다양성으로 위장해 들어오는 걸 막습니다. 기본은 fitness 음수 알파는 절대 못 들어옴.
- **최근성 관문(min_recent_sharpe)**: 최근 샤프(반감기 가중)가 낮으면 탈락. 옛날(2020~21)에만 벌고 요즘 죽은 알파를 거릅니다.
- **상관 관문(max_corr_threshold)**: 이미 뽑은 알파와 |상관|이 기준 이상이면 탈락(중복 제거).
- **계열 상한(max_per_family)**: 상관이 낮아도 같은 계열(예: funding 계열)을 너무 많이 쌓지 못하게 계열당 최대 개수를 제한.

> **현재 프로덕션 config(4alpha)** 는 서로 독립인 4개 알파를 씁니다: `taker_flow_follow`(플로우) + `range_high_60d`(모멘텀) + `crowd_ls_fade`(포지셔닝) + `funding_carry_7d`(펀딩). 넷 다 상관 0.5 미만으로 독립이고, 전부 OOS 검증을 통과했습니다.

### 6-2. 비중(weighting) — 얼마씩 주나

뽑은 알파에게 자본 비중을 나눠주는 방식(`weighting.method`)입니다. 세 가지가 있습니다.

- **equal(균등)**: 모두 똑같이.
- **inverse_vol(변동성 역수)**: 손익이 덜 출렁이는(꾸준한) 알파에게 더 많이. 단, 한 알파가 다 먹지 못하게 `max_weight`(예: 0.35)로 상한을 두고 초과분은 재분배.
- **skill(실력 비례)**: **최근 실력(반감기 가중 샤프)에 비례**해 비중을 줍니다. 잘하는 알파는 더 받고, 식은 알파는 비중이 부드럽게 0으로 수렴합니다.

**skill 가중의 핵심 아이디어(퇴출과 연결):** 알파를 "넣거나 빼거나"의 이진 스위치로 다루면 성적이 뚝뚝 끊깁니다. 대신 실력에 비례해 비중을 조절하면, 실력이 떨어진 알파는 **자동으로 벤치로 밀려나고(비중↓)**, 회복하면 **자동으로 복귀(비중↑)** 합니다. 즉 **퇴출이 별도 규칙이 아니라 비중 조절에 녹아 있습니다.** floor(하한) 밑으로 식은 알파는 기여가 0이 되어 부드럽게 빠집니다.

> 참고로 프로덕션에서 알파를 완전히 빼는 판단(예: amihud를 제외)은 사람이 config에서 하고, 그 안에서의 미세 조정은 selection의 최근성 관문 + weighting이 자동으로 합니다.

### 6-3. 합치기(combine) — 하나의 목표 비중으로

각 알파는 자기 나름의 비중표(코인별)를 냅니다. 이걸 알파 비중만큼 곱해서 더한 뒤, **매일 절대값 합이 1이 되도록(L1=1)** 다시 정규화합니다. 이렇게 하면 알파가 몇 개든 전체 포지션 크기(레버리지)가 일정하게 유지됩니다.

---

## 7. 리스크 로직 — 안전장치

합쳐진 목표 비중에 **브레이크를 씌우는 단계**입니다. 알파가 아무리 좋아도, 한 코인에 몰빵하거나 손실이 커질 때 규모를 줄여야 살아남습니다. 리스크 모듈들은 config의 `risk_pipeline`에 적힌 **순서대로** 하나씩 적용됩니다. 각 단계 후 성과가 어떻게 변했는지 리포트로 남겨, 어느 장치가 도움이 됐는지 눈으로 확인합니다.

현재 프로덕션(4alpha)의 리스크 순서와 뜻:

1. **position_cap (개별 코인 상한, 0.10)**: 어떤 코인도 전체 북의 ±10%를 못 넘게 자름. 한 코인 폭발 방지.
2. **market_neutrality (시장중립 밴드)**: 그날 롱숏 불균형(순노출)을 한계(±0.20) 안으로. 초과분을 그날 보유 코인들에 균등하게 덜어냄.
3. **family_corr_scale (계열 상관 축소)**: 같은 계열 알파들의 손익 상관이 갑자기 치솟으면 그 계열 비중을 줄임. 선택 시점엔 독립이었어도 나중에 같이 움직이기 시작하면 위험하므로.
4. **family_gross_cap (계열 총량 상한)**: 한 계열에 실린 총 포지션(Σ|비중|)의 하드 상한(0.5). 위 부드러운 축소의 백스톱.
5. **vol_target (변동성 목표, max_scale 1.5)**: 최근 실현 변동성이 목표(연 15%)를 넘으면 규모를 줄이고, 낮으면 키움. 단 과거 손익만 보고 계산해 미래참조 없음.
6. **killswitch_v2 (급락 차단기)**: 손실 속도가 빠르거나(10일 급락) 낙폭이 깊어지면 노출을 0으로 껐다가, 안정되면 며칠에 걸쳐 서서히(램프업) 복귀. 1세대의 "전고점 회복해야 재진입" 문제를 개선한 버전.
7. **gross_cap (총 레버리지 상한, 1.5)**: 전체 포지션 합(Σ|비중|)이 1.5를 넘으면 북 전체를 비례 축소. 어떤 조합으로도 몰래 레버리지가 걸리지 않게 하는 최종 안전장치.

그 밖에 준비된(선택적) 장치들:
- **participation_cap**: 각 코인 포지션이 그 코인의 하루 평균 거래대금(ADV)의 일정 비율을 못 넘게. 실제로 못 사고팔 만큼 크게 잡는 걸 방지.
- **downside_vol_target**: 손실 쪽 변동성에만 반응(이익으로 출렁이는 건 안 벌줌).
- **alpha_decay_gate**: 최근 추세 샤프가 0 밑으로 떨어지면 노출을 끔(느린 죽음 감지).
- **lot_rounding**: 실제 거래 가능한 최소 단위로 비중 반올림.

> **모든 리스크 계산은 "인과적(causal)"** 입니다. 즉 오늘의 브레이크는 **어제까지의 정보**로만 정합니다. 오늘 손실을 보고 오늘 미리 줄이는 식의 반칙(미래참조)이 없습니다.

### 방향성 규칙(directional) — 롱숏 비율을 신호가 정하게

기본은 모든 알파를 시장중립(5:5)으로 만들지만, 그러면 신호가 정말 한 방향에 확신이 있어도 그 정보가 죽습니다. 그래서 **자격을 갖춘 알파만** 방향성을 일부 허용하는 영속 규칙이 있습니다.

- 조건 (둘 다 만족): ① 알파의 계열이 자격 계열(예: 펀딩)이고, ② 원신호에 실제로 음수가 있어(방향성이 있는 signed 신호) rank/zscore로 감싼 상수 편향이 아닐 것.
- 통과하면 그 알파의 중립화를 `partial`로 자동 승격 → 순노출을 신호가 정하되, 전체 순노출은 위 market_neutrality 밴드가 통제.
- 이 규칙은 영속적입니다: 자격 있는 새 알파가 들어오면 자동 승격, 빠지면 자동 소멸.

---

## 8. 라이브 — 실제로 매일 매매하기

검증된 config를 매일 실행해 실제(또는 가상) 주문을 내는 단계입니다.

### 8-1. 하루 사이클 (handler.run_cycle)

```
1. (선택) 최신 데이터 갱신(refresh)
2. 신선도 검사(freshness) — 데이터가 안 들어온 알파는 제외
3. 오늘 목표 비중 계산(target_weights) — 백테스트 함수를 그대로 재사용해
   전체 히스토리로 combine+risk를 돌린 뒤 "가장 마지막 날 비중 = 오늘 목표"
4. 주문 생성/실행(orders) — 현재 포지션과 목표의 차이만큼 주문
5. 원장 기록(ledger) + 텔레메트리 스냅샷 저장
6. 텔레그램으로 결과 보고
```

### 8-2. 안전 설계(fail-safe)

- **신선도 게이트(freshness)**: 어떤 알파의 데이터가 2일 넘게 안 들어오면 그 알파를 제외합니다. **모든 알파가 stale(오래됨)이면 목표를 아예 내지 않고**, 주문을 SKIP해 **기존 포지션을 그대로 유지**합니다. 데이터가 끊긴 상태에서 엉뚱한 청산을 하지 않기 위함입니다.
- **리밸런싱 밴드(rebalance_band)**: 현재와 목표의 총 차이가 작으면 굳이 거래하지 않습니다(수수료 절약).
- **일일 가드(daily guard)**: 실매매 주문이 오늘 이미 나갔으면 중복 전송하지 않습니다.
- **가상/실매매 모드**: `paper`(가상, 기본)와 `real`(실매매)이 있고, 실매매여도 가상 손익을 항상 같이 추적합니다.

### 8-3. 실시간 가상 손익 (/잔고 의 비밀)

가상매매 손익은 원래 '하루 한 번'만 계산됩니다(사이클 때 그날 수익률로 한 칸 기록). 그런데 텔레그램에서 /잔고 를 누르면 **지금 이 순간**의 손익이 나옵니다. 원리는 이렇습니다.

1. 리밸런싱 직후, 보유 코인들의 그 순간 가격을 **진입가 스냅샷**(positions_entry.json)으로 저장해 둡니다.
2. /잔고 요청이 오면 지금 가격을 다시 조회해서, `보유비중 × (지금가격 ÷ 진입가 − 1)` 를 전부 더합니다.
3. 어제 마감 기준 누적 손익에 이 "오늘 장중 변동"을 얹어 보여줍니다.

가격 조회가 실패하면 조용히 저장된 일일 값으로 되돌아갑니다(fail-open).

### 8-4. 텔레그램 봇 — 보고와 원격 제어

`telegram_bot`이 하는 일은 크게 세 가지입니다.

- **자동 보고**: 매 사이클이 끝나면 결과를 **그림 대시보드(PNG)** 로 보내줍니다(총자산·오늘 손익, 최근30일/최대낙폭/승률 지표, 자산 곡선과 '비트코인 그냥 보유' 비교선, 일별 손익 막대, 롱숏 균형, 오늘의 매매, 코인별 손익 기여). 그림 생성이 실패하면 텍스트 요약으로 대신 보냅니다 — 어떤 경우에도 보고는 옵니다.
- **원격 제어**: 봇 켜기/끄기(/토글), 가상↔실매매 전환(/모드), 즉시 실행(/실행), 범위별 초기화(/초기화 포지션|잔고|전체 확인 — 클라우드 R2의 기록까지 같이 지워 다음 사이클에 되살아나지 않게 처리).
- **조회**: 상태·포지션·잔고·리스크 설정은 물론, **"이 코인 왜 샀어?"(/이유)** 와 **"끝난 매매 목록"(/기록)** 까지. 전체 명령어 표는 `구조_및_명령어_가이드.md` 참고.

**보안**: `.env`의 `TELEGRAM_ALLOWED_CHAT_ID` 에 등록된 채팅에서 온 명령만 처리합니다. 다른 사람이 봇 주소를 알아내도 명령이 통하지 않습니다.

**명령을 받는 두 가지 방식:**

- **폴링(polling)**: 로컬 PC에서 `python cli.py bot` 을 켜두면, 봇이 텔레그램 서버에 "새 메시지 있어요?"를 계속 물어봅니다. 컴퓨터가 켜져 있어야 합니다.
- **웹훅(webhook)**: 텔레그램이 새 메시지를 우리 **Lambda 함수 주소(Function URL)** 로 직접 쏴줍니다. 서버 없이도 24시간 응답합니다(현재 운영 방식). 위장 요청을 막기 위해 비밀 토큰(`TELEGRAM_WEBHOOK_SECRET`)을 매 요청마다 대조하고, 무거운 /실행 은 즉시 돌리지 않고 "접수" 안내만 한 뒤 예약된 자동 사이클에 맡깁니다(웹훅은 몇 초 안에 응답해야 텔레그램이 재시도하지 않기 때문).

### 8-5. 텔레메트리 — 블랙박스(플라이트 레코더)

매 사이클마다 그날의 상세 스냅샷을 **telemetry-날짜.json** 으로 남깁니다. 담기는 것: 목표 비중, 리스크 적용 전/후 비중, **알파별 코인별 기여분**, 그날 실현 수익률, 주문 내역. 이 파일 하나로 나중에 백테스트를 다시 돌리지 않고도 다음을 알 수 있습니다.

- **알파 기여도**: 어떤 알파가 실제로 돈을 벌었나 (`python cli.py attribution <파일>`)
- **리스크 효과**: 안전장치가 수익을 깎았나(Drag) 지켜줬나(Benefit)
- **/이유**: 특정 코인을 왜 이 방향·비중으로 잡았는지 (텔레그램에서 즉답)
- **/기록**: 진입→종료가 끝난 매매의 시간순 목록과 건별 손익 (텔레그램에서 즉답)

핵심 타이밍은 **lag-1**: T일에 정한 목표가 T+1일 수익률을 법니다. 파일은 90일 지나면 자동 정리됩니다.

---

## 9. 클라우드에서 24시간 돌리기 — Lambda + R2

내 컴퓨터를 24시간 켜두지 않아도 매일 매매가 돌아가는 구조입니다.

### 9-1. 등장인물 세 명

- **AWS Lambda**: "서버 없는 서버". 우리 프로그램(도커 이미지)을 올려두면, 필요할 때만 깨어나 실행되고 끝나면 잠듭니다. 실행한 시간만큼만 과금(거의 무료 수준).
- **EventBridge 크론**: 알람시계. 매일 정해진 시간에 Lambda를 깨워 "오늘 사이클 돌려"라고 시킵니다.
- **Cloudflare R2**: 창고(저장소). Lambda는 실행이 끝나면 기억이 싹 지워지므로, 매매 데이터와 상태를 바깥 창고에 보관해야 합니다. R2는 아마존 S3와 똑같은 방식으로 말이 통하는(S3 호환 API) 저장소인데 **무료 용량(10GB)** 이 있어 선택했습니다.

### 9-2. 왜 이런 구조가 필요했나

Lambda의 파일 시스템은 `/tmp` 폴더 하나만 쓸 수 있고, 그마저 실행이 끝나면 **싹 지워집니다**. 그냥 돌리면 매일 몇 년치 데이터를 처음부터 다시 받다가 15분 제한시간에 걸려 죽습니다. 그래서:

1. 시작할 때: R2 창고에서 **어제까지의 데이터**를 `/tmp` 로 내려받고,
2. **오늘치만 증분 수집**해서 이어붙인 뒤, 패널을 다시 만들고 매매하고,
3. 끝날 때: 갱신된 데이터와 매매 상태(포지션·손익·텔레메트리)를 **다시 R2로 올립니다**.

첫 시작만큼은 로컬에서 초기 데이터를 R2에 한 번 밀어넣어 줍니다(`python -m src.live.remote_store push`) — 전체 7.7GB가 아니라 라이브에 필요한 스코프(채택 알파가 쓰는 데이터셋 × top-100 심볼, 약 1.6GB)만 올라갑니다.

### 9-3. 하루 동안 실제로 일어나는 일

```
매일 (EventBridge 알람)                     아무 때나 (텔레그램 명령)
────────────────────────                    ────────────────────────
Lambda 깨어남                                텔레그램 → 웹훅 → Lambda 깨어남
 1. R2 → /tmp 데이터 다운로드                 1. 비밀 토큰 검증
 2. 오늘치 데이터 증분 수집                    2. 그 명령에 필요한 파일만
 3. 패널 재빌드                                  R2에서 콕 집어 다운로드(빠름)
 4. 목표 비중 계산 → 주문                      3. 명령 처리 → 답장 전송
 5. 텔레메트리·손익 기록                       4. 상태를 바꾼 명령(/모드 등)이면
 6. /tmp → R2 업로드                             R2에 다시 업로드
 7. 텔레그램으로 그림 보고서 발송
```

크론 사이클에는 안전장치가 있습니다: 데이터 수집이 오래 걸려도 **마감시각(deadline)** 을 정해두고, 시간이 모자라면 수집을 끊고 남은 시간에 매매·보고를 반드시 진행합니다(15분 타임아웃으로 통째로 죽는 것 방지).

### 9-4. 상태 파일은 무엇이 오가나

R2에 왕복 보관되는 것들(전부 `data/` 아래 상대경로 그대로 창고의 물건 이름이 됩니다):

- `market/processed·panel·universe·scan` — 시장 데이터(증분 수집이 이어붙는 원본과 캐시)
- `runtime/live/config.json` — 봇 켜짐/꺼짐, 가상/실매매 모드
- `runtime/live/positions.json` — 현재 보유 포지션
- `runtime/live/positions_entry.json` — 실시간 손익용 진입가 스냅샷
- `runtime/live/paper_equity.jsonl` — 누적 가상 자산 곡선
- `runtime/live/orders_날짜.json`, `telemetry/telemetry-날짜.json` — 날짜별 매매 기록(90일 보존)

> R2 대신 Oracle Cloud·Backblaze 같은 다른 S3 호환 저장소로 바꾸고 싶으면 접속 주소·키 환경변수 4개만 갈아끼우면 됩니다. 코드는 안 바뀝니다.
>
> **실제 배포 명령어(도커 빌드→ECR→Lambda 설정)와 자주 막히는 지점은 `구조_및_명령어_가이드.md` 의 배포 장에 있습니다.**

---

## 10. 모듈(폴더)별 기능 지도

프로그램의 코드는 `src/` 아래 네 덩어리로 나뉩니다.

### `src/collector/` — 데이터 수집
- **symbol_universe**: 거래 가능한 심볼 목록 만들기.
- **universe_probe**: 유니버스 판단용 거래대금 점수(rolling_score) 스캔.
- **universe_builder**: 과거 매 시점의 유니버스 스냅샷 복원(생존편향 방지).
- **universe_maintenance**: 월간 유니버스 갱신 체인.
- **full_collector**: 유니버스에 편입된 적 있는 모든 심볼의 데이터를 빈틈 없이(gap-aware) 수집.
- **live_refresh**: 라이브용 최신 데이터만 빠르게 갱신.
- **shared/**: 아카이브·REST 클라이언트, 저장, 요청 속도 제한(throttle).

### `src/backtest/` — 알파 연구
- **spec**: 알파 정의(AlphaSpec)와 검증.
- **operators**: 수식에 쓰는 연산자들(rank, zscore, ts_mean...).
- **evaluate**: 수식 문자열을 안전하게 계산.
- **panel**: 코인별 파일 → 날짜×코인 표(패널)로 변환·캐시, 유니버스 마스크, 8시간 펀딩 이벤트 로드.
- **timegrid**: 서로 다른 판단 주기(bar)를 마스터 그리드로 맞추기.
- **engine**: 알파 하나 → 포지션·순손익(위 손익 회계).
- **metrics**: 샤프·IC·회전율·MDD·fitness 등 점수 계산.
- **validation**: OOS/워크포워드/순열검정.
- **walkforward**: 선택+비중+리스크 전 과정의 미래참조-0 OOS 재검증.
- **directional**: 방향성 규칙(롱숏 비율을 신호가 정하게).

### `src/portfolio/` — 포트폴리오 구성
- **spec**: 포트폴리오 config 정의.
- **combine**: 알파 선택(selection)·비중(weighting)·합치기.
- **risk**: 리스크 안전장치 모듈들과 파이프라인.
- **pipeline**: config → 백테스트 전 과정 실행(선택→합치기→리스크→최종손익).
- **report**: 리스크 단계별 성과 리포트.

### `src/live/` — 실매매
- **freshness**: 데이터 신선도 검사.
- **target_weights**: 오늘의 목표 비중 계산(백테스트 함수 재사용).
- **orders**: 목표와 현재의 차이로 주문 생성/실행.
- **handler**: 하루 사이클 전체 조율(엔트리포인트).
- **ledger**: 원장·텔레메트리 기록·보존기간 정리.
- **paper**: 가상매매 손익 추적.
- **live_pnl**: 진입가 스냅샷 대비 실시간 가상 손익(/잔고).
- **state**: 라이브 설정(켜기/끄기·모드) 상태.
- **attribution**: 텔레메트리로 알파·리스크 기여도 역추적.
- **trade_history**: 텔레메트리로 끝난 매매(진입→종료→손익) 복원(/기록).
- **dashboard**: 사이클 결과를 그림(PNG) 대시보드로 렌더링.
- **telegram_bot**: 텔레그램 보고·원격제어·조회 명령(/이유, /기록 포함).
- **remote_store**: R2/S3 호환 저장소 동기화(클라우드 영속화).
- **exchange/**: 바이낸스 주문 실행 클라이언트.

### 진입점 두 개
- **`cli.py`** — 로컬에서 쓰는 모든 기능의 진입점(`python cli.py <명령>`). **전체 명령어 사용법은 `구조_및_명령어_가이드.md` 에 있습니다.**
- **`lambda_handler.py`** — 클라우드(Lambda) 진입점. EventBridge 크론이 부르면 하루 사이클을, 텔레그램 웹훅이 부르면 명령 응답을 처리합니다.

---

## 11. 데이터 폴더 구조

```
data/
├── market/          시장 데이터
│   ├── (심볼별 원자료)         klines/metrics/funding/premium
│   ├── panel/                  날짜×코인 표 캐시(백테스트용)
│   ├── universe/               과거 시점별 유니버스 스냅샷
│   │   └── rules/              유니버스 선정 규칙
│   └── scan/                   유니버스 판단용 점수
├── strategy/        전략 정의
│   ├── alphas/                 알파 JSON들(알파 1개 = 파일 1개)
│   ├── portfolio/config.json   현재 프로덕션 포트폴리오 설정
│   └── meta/
│       ├── symbol_list.json        심볼 목록
│       ├── alpha_families.json     알파 계열 분류
│       └── directional_policy.json 방향성 규칙 설정
└── runtime/         실행 산출물
    ├── live/
    │   ├── config.json             봇 켜짐/꺼짐·모드 (텔레그램으로 변경 가능)
    │   ├── positions.json          현재 가상 포지션
    │   ├── positions_entry.json    실시간 손익용 진입가 스냅샷
    │   ├── paper_equity.jsonl      누적 가상 자산 곡선 (하루 한 줄)
    │   ├── orders_날짜.json        날짜별 주문 기록 (90일 보존)
    │   └── telemetry/              날짜별 상세 스냅샷 = 블랙박스 (90일 보존)
    └── logs/                   리포트·이벤트 로그
```

---

## 12. 핵심 설계 원칙 요약

이 프로그램을 관통하는 몇 가지 원칙입니다.

1. **미래참조 0** — delay≥1, 모든 리스크·비중 계산은 인과적(어제까지 정보만). 백테스트가 실전과 어긋나지 않게 하는 뿌리 원칙.
2. **순손익 기준** — 모든 점수는 수수료·슬리피지·8시간 펀딩을 뺀 진짜 손익에서 계산.
3. **가설 있는 알파만** — 아무 숫자나 붙이지 않고, 경제적 이유가 있는 규칙만. 필드 마이닝(마구잡이 조합)은 레짐 과최적화 위험.
4. **독립 베팅의 수가 힘** — 알파 개수가 아니라 서로 안 겹치는 베팅의 수가 포트폴리오를 강하게 만듦.
5. **부드러운 전환** — 알파 퇴출/복귀를 이진 스위치가 아니라 실력 비례 비중으로. 리스크도 급락 시 껐다가 서서히 복귀.
6. **fail-safe 우선** — 데이터가 끊기면 엉뚱한 거래 대신 포지션 유지. 안전이 수익보다 먼저.
7. **백테스트=라이브 코드 재사용** — 라이브는 별도 엔진이 아니라 백테스트 함수를 그대로 써서 "마지막 날 비중"을 오늘 목표로 삼음. 두 세계가 어긋날 여지를 없앰.

---

## 13. 함께 보는 문서

- **`구조_및_명령어_가이드.md`** — 실제 폴더 구조, `cli.py` 전체 명령어 사용법, 텔레그램 명령어 표, 로컬 백테스트/데이터 수집 방법, 도커 이미지 빌드와 Lambda 배포 절차, 자주 막히는 지점. "어떻게 돌리나"는 전부 그쪽에 있고, 이 문서(설명서)는 "무엇이 어떻게 동작하나"를 담당합니다.   





























