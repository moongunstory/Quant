# Quant 구조 및 명령어 가이드

> 이 문서는 **"어떻게 돌리나"** 를 담당합니다. 폴더 구조, `cli.py` 전체 명령어, 텔레그램 명령어,
> 데이터 수집·백테스트·배포 방법을 비전공자도 따라 할 수 있게 적었습니다.
> 프로그램이 **"무엇을 어떻게 하는지"**(개념·원리)는 `프로그램_설명서.md` 를 보세요.

---

## 1. 폴더 구조 — 어디에 뭐가 있나

```
Quant/
├── cli.py               ★ 로컬에서 쓰는 모든 명령의 입구 (python cli.py <명령>)
├── lambda_handler.py    클라우드(AWS Lambda) 입구 — 크론 사이클과 텔레그램 웹훅 처리
├── Dockerfile           Lambda 에 올릴 도커 이미지 설계도
├── requirements.txt     필요한 파이썬 라이브러리 목록
├── .env                 API 키·텔레그램 토큰 등 비밀값 (절대 공유 금지)
│
├── src/                 프로그램 본체 (코드)
│   ├── collector/       데이터 수집 (유니버스 선정, 시장 데이터 내려받기)
│   ├── backtest/        알파 연구 (수식 → 신호 → 손익, 검증)
│   ├── portfolio/       알파 조합 + 리스크 안전장치
│   ├── live/            실매매 (목표 비중, 주문, 텔레그램, 클라우드 동기화)
│   └── config/          설정 (경로, 백테스트 상수, 거래소 API)
│
└── data/                데이터 (코드 아님)
    ├── market/          시장 데이터 (원본·패널 캐시·유니버스 스냅샷)
    ├── strategy/        전략 정의 (알파 JSON, 포트폴리오 config)
    └── runtime/         실행하며 쌓이는 것 (포지션, 손익, 텔레메트리, 로그)
```

한 줄 요약: **코드는 `src/`, 데이터는 `data/`, 실행은 `cli.py`** 입니다.

---

## 2. 처음 준비 (한 번만)

1. 파이썬 3.11 이상 설치.
2. 라이브러리 설치:
   ```bash
   cd C:\Users\moong\Desktop\Quant
   pip install -r requirements.txt
   ```
3. `.env` 파일 채우기 (프로젝트 루트에 위치):

| 항목 | 뜻 |
|---|---|
| `TRADING_MODE` | `testnet`(연습 계좌) 또는 `live`(실계좌) |
| `BINANCE_API_KEY` / `BINANCE_SECRET_KEY` | 바이낸스 실계좌 키 |
| `TESTNET_API_KEY` / `TESTNET_SECRET_KEY` | 바이낸스 테스트넷(연습) 키 |
| `TELEGRAM_BOT_TOKEN` | 텔레그램 봇 토큰 (BotFather 에서 발급) |
| `TELEGRAM_CHAT_ID` | 보고를 받을 채팅 ID |
| `TELEGRAM_ALLOWED_CHAT_ID` | 명령을 허용할 채팅 ID (보안 — 보통 위와 동일) |
| `TELEGRAM_WEBHOOK_SECRET` | 웹훅 위장 요청 차단용 비밀 문자열 (아무 긴 문자열) |
| `REMOTE_STORE` | 클라우드 저장소 사용 여부 (`1`=사용) — 로컬만 쓰면 비워둠 |
| `S3_ENDPOINT_URL` / `S3_BUCKET` / `S3_ACCESS_KEY_ID` / `S3_SECRET_ACCESS_KEY` / `S3_REGION` | R2(또는 S3 호환) 저장소 접속 정보 |

---

## 3. cli.py 전체 명령어 사전

모든 명령은 프로젝트 폴더에서 `python cli.py <명령>` 형태로 실행합니다.
`python cli.py --help`, `python cli.py <명령> --help` 로 언제든 도움말을 볼 수 있습니다.

### 3-1. 데이터 수집 계열

**`universe-refresh` — 월간 유니버스 갱신 (월 1회 권장)**
"이번 달에 거래할 후보 코인 top-100"을 새로 뽑는 3단계(심볼 목록 → 거래대금 스캔 → 월별 스냅샷)를 한 번에 돌립니다.
```bash
python cli.py universe-refresh
```

**`universe` / `universe-probe` / `universe-builder` — 위 3단계를 따로따로**
보통은 `universe-refresh` 하나면 되고, 문제가 생겨 단계별로 다시 돌릴 때만 씁니다.
- `universe`: 거래 가능한 심볼 목록 갱신
- `universe-probe`: 유니버스 판단용 거래대금 점수 스캔
- `universe-builder`: 과거 매 시점의 유니버스 스냅샷 재구성 (생존편향 방지용)

**`full-collector` — 시장 데이터 전체 수집**
유니버스에 편입된 적 있는 모든 코인의 데이터를 빈틈 없이 내려받습니다. 이미 받은 날짜는 건너뛰므로(gap-aware) 여러 번 돌려도 안전하고, 중간에 끊겨도 이어받습니다.
```bash
python cli.py full-collector                      # 기본 4종 데이터셋
python cli.py full-collector --datasets bookDepth  # 특정 데이터셋만
python cli.py full-collector --workers 5           # 병렬 수 조절 (기본 10)
```
> 처음부터 전부 받으면 수 시간~ 걸립니다. 이후에는 빠진 부분만 받아서 빠릅니다.

### 3-2. 백테스트 계열

**`backtest-build-panel` — 패널 캐시 만들기**
코인별 원본 파일을 백테스트가 쓰는 "날짜×코인 표(패널)"로 변환해 저장합니다. 수집 후 한 번 돌려두면 이후 백테스트가 빨라집니다.
```bash
python cli.py backtest-build-panel             # 전체 필드
python cli.py backtest-build-panel --rebuild   # 캐시 무시하고 다시 만들기
```

**`backtest-test` — 알파 하나 시험**
```bash
python cli.py backtest-test --alpha funding_carry_7d
python cli.py backtest-test --alpha funding_carry_7d --validate   # 우연인지 검증까지
python cli.py backtest-test --alpha funding_carry_7d --save-curve # 자본곡선 CSV 저장
```
샤프·수익률·회전율·IC 등의 성적표가 나옵니다. 각 숫자의 뜻은 설명서 4장 참고.

**`backtest-rank` — 모든 알파 줄 세우기**
`data/strategy/alphas/` 의 알파 전부를 백테스트해 fitness 순위표로 보여줍니다.
```bash
python cli.py backtest-rank
python cli.py backtest-rank --only breakout_uncrowded reversal_5d  # 지정한 것만 (새 알파 빠른 확인)
```

**`backtest-corr` — 알파끼리 겹치는지 확인**
알파들 손익의 상관관계를 재서 "진짜 서로 다른 베팅이 몇 개인지(독립 그룹)"를 셉니다.
```bash
python cli.py backtest-corr
python cli.py backtest-corr --threshold 0.4 --save   # 판정 기준 조절 + CSV 저장
```

**`backtest-all` — 일괄 실행**
패널 빌드 → 전체 알파 백테스트 → 랭킹까지 한 번에.
```bash
python cli.py backtest-all --validate
```

**`portfolio` — 포트폴리오(알파 조합+리스크) 백테스트**
현재 프로덕션 설정이 과거에 어떤 성적이었는지, 리스크 안전장치 단계별로 성과가 어떻게 변하는지 보여줍니다.
```bash
python cli.py portfolio                                    # 기본 config
python cli.py portfolio --config data/strategy/portfolio/config.json --save-report
```

**`walkforward` — 가장 엄격한 검증 (미래참조 0)**
"알파 선택→비중→리스크"의 전 과정을 매 시점 그때까지의 데이터만으로 다시 실행해, 실전과 똑같은 조건의 성적(OOS)을 냅니다. 포트폴리오를 바꿨으면 이걸로 최종 확인하세요.
```bash
python cli.py walkforward --config data/strategy/portfolio/config.json --save-report
```

### 3-3. 라이브(실매매) 계열

**`live` — 라이브 사이클 한 번 돌리기 (로컬)**
데이터 최신화 → 오늘 목표 비중 → 주문 → 기록 → 텔레그램 보고까지 하루치 전 과정.
```bash
python cli.py live --refresh                 # 데이터 최신화 포함 (권장)
python cli.py live --refresh --mode paper    # 가상매매 강제
python cli.py live --refresh --mode real     # 실매매 강제 (조심!)
```
`--mode` 를 생략하면 텔레그램 /모드 로 정한 현재 모드를 따릅니다.

**`trade` — 수동 주문 (테스트용)**
자동매매와 무관하게 주문 하나를 직접 넣어봅니다. 거래소 연결 확인용.
```bash
python cli.py trade --symbol BTCUSDT --side BUY --quantity 0.001 --mode testnet
```

**`bot` — 텔레그램 봇 켜기 (로컬 폴링 방식)**
컴퓨터를 켜두는 동안 텔레그램 명령에 응답합니다. (클라우드 웹훅을 쓰면 필요 없음)
```bash
python cli.py bot
```

**`set-webhook` — 텔레그램 웹훅 등록/해제 (클라우드 방식)**
텔레그램 명령을 Lambda 가 24시간 받게 연결합니다. 배포(6장) 후 한 번만.
```bash
python cli.py set-webhook --url https://<함수ID>.lambda-url.<리전>.on.aws/
python cli.py set-webhook --info      # 현재 상태 확인
python cli.py set-webhook --delete    # 해제 (로컬 폴링으로 복귀)
```

**`telemetry-send` — 텔레메트리 묶음 전송**
최근 N일의 상세 기록(블랙박스)을 zip 으로 묶어 텔레그램으로 보냅니다.
```bash
python cli.py telemetry-send --days 30
python cli.py telemetry-send --days 30 --no-send   # 전송 없이 zip 만 생성
```

**`attribution` — 기여도 분석 (어느 알파가 돈 벌었나)**
텔레그램으로 받은 텔레메트리 zip(또는 폴더)을 넣으면 알파별 실제 손익 기여와 리스크 장치의 효과(Drag/Benefit)를 표·CSV·그래프로 보여줍니다.
```bash
python cli.py attribution telemetry-bundle-2026-06-18_2026-07-17.zip --out-dir ./분석결과
```

### 3-4. 클라우드 저장소(R2) 동기화

`cli.py` 가 아니라 모듈 직접 실행 방식입니다 (`.env` 에 `REMOTE_STORE=1` 과 `S3_*` 필요):
```bash
python -m src.live.remote_store push   # 로컬 데이터를 R2 로 (최초 1회 초기 주입)
python -m src.live.remote_store pull   # R2 데이터를 로컬로 (점검용)
python -m src.live.remote_store list   # R2 에 뭐가 올라가 있나 확인
```

---

## 4. 자주 하는 작업 레시피

**A. 맨 처음 데이터 갖추기 (순서대로)**
```bash
python cli.py universe-refresh        # ① 거래 후보 코인 정하기
python cli.py full-collector          # ② 시장 데이터 전체 수집 (오래 걸림)
python cli.py backtest-build-panel    # ③ 백테스트용 표 캐시 만들기
```

**B. 매달 유지보수**
```bash
python cli.py universe-refresh        # 유니버스 갱신 (월 1회)
python cli.py full-collector          # 빠진 데이터 이어받기
```

**C. 새 알파 아이디어 시험하기**
1. `data/strategy/alphas/새이름.json` 파일 작성 (기존 파일 복사해서 수정이 쉬움)
2. `python cli.py backtest-test --alpha 새이름 --validate`
3. 좋아 보이면 `python cli.py backtest-corr` 로 기존 알파와 겹치는지 확인
4. config 에 넣었다면 `python cli.py walkforward --config ...` 로 최종 검증

**D. 로컬에서 라이브 하루 돌려보기**
```bash
python cli.py live --refresh --mode paper
```

**E. 실매매 전환**
텔레그램에서 `/모드 실매매` (또는 Lambda 환경변수 `TRADING_MODE=live`). 되돌리려면 `/모드 가상매매`.

---

## 5. 텔레그램 명령어 전체표

텔레그램 봇 채팅창에 입력합니다. `TELEGRAM_ALLOWED_CHAT_ID` 로 등록된 채팅에서만 작동합니다.

### 조회 (읽기만 함)

| 명령어 | 하는 일 |
|---|---|
| `/상태` | 봇 켜짐/꺼짐, 매매 모드, 마지막 매매일, 크게 베팅한 종목 5개 |
| `/포지션` | 지금 들고 있는 전체 종목과 비중 (롱/숏 표시) |
| `/잔고` | 지금 이 순간의 가상 손익 (진입가 대비 실시간 계산) |
| `/이유 <코인>` | **그 코인을 왜 이 방향·이 비중으로 잡았나** — 전략별 의견 + 안전장치 조정 내역. 예: `/이유 BTC` |
| `/기록 [개수\|코인]` | **진입→종료가 끝난 매매 목록** (시간순, 건별 손익). 예: `/기록`, `/기록 30`, `/기록 BTC` |
| `/리스크` | 지금 켜져 있는 리스크 안전장치 목록과 설정값 |
| `/스냅샷` | 이번 달 유니버스(거래 후보 100종목) 정보 |
| `/텔레메트리 [일수\|날짜]` | 상세 기록(블랙박스) 파일 전송. 예: `/텔레메트리 7`, `/텔레메트리 2026-07-13` |
| `/도움말` | 명령어 목록 |

### 제어 (상태를 바꿈)

| 명령어 | 하는 일 |
|---|---|
| `/토글` | 자동매매 켜기 ↔ 끄기 |
| `/모드 실매매` / `/모드 가상매매` | 실제 돈 ↔ 가상 돈 전환 |
| `/실행` | 사이클 즉시 실행 (로컬 봇) / 접수 안내 (클라우드 웹훅) |
| `/초기화 <범위> 확인` | 범위를 골라 초기화. `포지션`(보유+진입가만) / `잔고`(+수익곡선) / `전체`(+매매기록·텔레메트리). 실수 방지를 위해 `확인`을 붙여야 실행되고, 범위 없이 치면 사용법이 나옵니다 |

> 매일 사이클이 끝나면 자동으로 **그림 보고서(대시보드 PNG)** 가 옵니다 — 명령 없이도.

---

## 6. 클라우드 배포 — 처음부터 끝까지

내 컴퓨터 없이 24시간 돌아가게 만드는 절차입니다. `<...>` 만 본인 값으로 바꾸세요.
(구조가 왜 이런지는 설명서 9장 참고.)

### 6-1. Cloudflare R2 버킷 + 키 만들기 (최초 1회)
1. Cloudflare 대시보드 → **R2** → **Create bucket** → 이름 예: `quant-live`
2. R2 첫 화면의 **Account ID** 복사 → 접속 주소는 `https://<계정ID>.r2.cloudflarestorage.com`
3. **Manage R2 API Tokens** → **Create API token** → 권한 **Object Read & Write** → **Access Key ID** 와 **Secret Access Key** 복사 (한 번만 보여줌!)

### 6-2. 로컬 `.env` 채우기
```
REMOTE_STORE="1"
S3_ENDPOINT_URL="https://<계정ID>.r2.cloudflarestorage.com"
S3_BUCKET="quant-live"
S3_ACCESS_KEY_ID="<Access Key ID>"
S3_SECRET_ACCESS_KEY="<Secret Access Key>"
S3_REGION="auto"
```

### 6-3. 초기 데이터 R2 에 넣기 (최초 1회)
Lambda 가 첫날부터 몇 년치를 받다 죽지 않도록, 로컬에서 미리 올려줍니다.
```bash
python -m src.live.remote_store push   # 라이브에 필요한 스코프만 (~1.6GB, 수 분)
python -m src.live.remote_store list   # 올라간 개수 확인
```

### 6-4. 도커 이미지 빌드 & ECR 푸시

**내 AWS 정보:**
| 항목 | 값 |
|---|---|
| AWS 계정 ID | `324471170327` |
| 리전 | `ap-northeast-2` (서울) |
| ECR 저장소 | `quant-live-cycle` |

```bash
# (최초 1회) ECR 저장소 생성
aws ecr create-repository --repository-name quant-live-cycle --region ap-northeast-2

# ECR 로그인 (만료되면 재실행)
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com

# 빌드 — 두 옵션 모두 필수!
#   --platform linux/amd64 : Lambda 는 amd64 만 지원
#   --provenance=false     : 최신 Docker 가 만드는 OCI 형식을 Lambda 가 못 읽음
docker build --platform linux/amd64 --provenance=false -t quant-live-cycle:latest .

# 태그 & 푸시
docker tag quant-live-cycle:latest 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com/quant-live-cycle:latest
docker push 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com/quant-live-cycle:latest
```

### 6-5. Lambda 함수 설정 (최초 1회)
처음이면 함수 생성: `aws lambda create-function --package-type Image --code ImageUri=<이미지주소> --role <실행역할ARN> ...`
그다음 리소스와 환경변수:
```bash
aws lambda update-function-configuration \
  --function-name <Lambda함수명> \
  --region <리전> \
  --timeout 900 \
  --memory-size 2048 \
  --ephemeral-storage '{"Size": 4096}' \
  --environment "Variables={
    QUANT_DATA_DIR=/tmp/quant-data,
    REMOTE_STORE=1,
    S3_ENDPOINT_URL=https://<계정ID>.r2.cloudflarestorage.com,
    S3_BUCKET=quant-live,
    S3_ACCESS_KEY_ID=<...>,
    S3_SECRET_ACCESS_KEY=<...>,
    S3_REGION=auto,
    TRADING_MODE=testnet,
    BINANCE_API_KEY=<...>,
    BINANCE_SECRET_KEY=<...>,
    TESTNET_API_KEY=<...>,
    TESTNET_SECRET_KEY=<...>,
    TELEGRAM_BOT_TOKEN=<...>,
    TELEGRAM_CHAT_ID=<...>,
    TELEGRAM_ALLOWED_CHAT_ID=<...>,
    TELEGRAM_WEBHOOK_SECRET=<...>,
    QUANT_RETENTION_DAYS=90
  }"
```
> 타임아웃 900초(15분)·메모리 2048MB·임시저장소 4096MB 는 데이터 수집+패널 재빌드를 위한 값입니다.
> 실계좌로 갈 땐 `TRADING_MODE=live` 로만 바꾸면 됩니다.

### 6-6. 매일 자동 실행 (EventBridge 크론)
AWS 콘솔 → EventBridge → Rules → 스케줄 규칙 생성(예: 매일 UTC 00:10) → 대상으로 이 Lambda 지정.

### 6-7. 텔레그램 웹훅 연결
Lambda 콘솔에서 **Function URL** 을 만들고(인증 NONE), 그 주소를 등록:
```bash
python cli.py set-webhook --url https://<함수ID>.lambda-url.<리전>.on.aws/
```
비밀 토큰은 `.env` 의 `TELEGRAM_WEBHOOK_SECRET` 를 자동으로 쓰며, Lambda 환경변수와 같은 값이어야 합니다.

### 6-8. 코드 고친 뒤 재배포 (앞으로 매번 이것만)
```bash
docker build --platform linux/amd64 --provenance=false -t quant-live-cycle:latest .
docker tag quant-live-cycle:latest 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com/quant-live-cycle:latest
docker push 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com/quant-live-cycle:latest

aws lambda update-function-code \
  --function-name <Lambda함수명> \
  --region ap-northeast-2 \
  --image-uri 324471170327.dkr.ecr.ap-northeast-2.amazonaws.com/quant-live-cycle:latest
```
> `<Lambda함수명>` 은 본인이 설정한 Lambda 함수 이름을 넣으세요.

### 6-9. 동작 확인
```bash
aws lambda invoke --function-name <Lambda함수명> --region ap-northeast-2 out.json && type out.json
```
> `<Lambda함수명>` 은 본인이 설정한 Lambda 함수 이름을 넣으세요.

`{"message":"success", ...}` 가 나오면 정상이고 텔레그램으로도 보고가 옵니다.
CloudWatch 로그에서 "R2 다운로드 완료 N개" / "R2 업로드 완료 N개" 가 찍히면 동기화 정상입니다.

---

## 7. 자주 막히는 지점 (문제 해결)

- **첫 실행이 `config.json 없음`/패널 에러** → 6-3 `push` 를 안 했거나 R2 키/버킷명이 Lambda 환경변수와 불일치. `python -m src.live.remote_store list` 로 R2 에 파일이 있는지부터 확인.
- **`No space left on device` / `/tmp` 오류** → 6-5 의 `--ephemeral-storage` 를 안 키운 경우.
- **첫 실행이 타임아웃** → 초기 주입(6-3)을 안 해서 Lambda 가 전체 백필을 시도한 것. 로컬 push 먼저.
- **`docker push` 권한 오류** → ECR 로그인 재실행 (로그인은 만료됨).
- **`exec format error`** → 빌드 시 `--platform linux/amd64` 누락.
- **`update-function-code` 에서 `image manifest ... media type ... is not supported`** → 빌드에 `--provenance=false` 누락. 붙여서 재빌드·재푸시. 그래도 나면 Docker Desktop → Settings → General → "Use containerd for pulling and storing images" 체크 해제 후 재빌드.
- **`pip` 가 소스빌드로 빠져 `gcc`/`c++ ... No such file` 에러** → 해당 패키지를 범용 휠(manylinux2014)이 있는 버전으로 `requirements.txt` 에 고정 (예: `contourpy==1.2.1`, `pillow==10.4.0`, `kiwisolver==1.4.5`). 확인 방법: `pip download --only-binary=:all: --python-version 3.11 --platform manylinux2014_x86_64 <패키지==버전>`
- **텔레그램 명령에 응답이 없음** → `python cli.py set-webhook --info` 로 웹훅 등록 확인. `TELEGRAM_WEBHOOK_SECRET` 이 로컬 `.env` 와 Lambda 환경변수에서 다르면 조용히 무시됩니다.
- **오라클/백블레이즈로 저장소 교체** → `S3_ENDPOINT_URL`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_REGION` 4개만 교체. 코드 변경 없음.
