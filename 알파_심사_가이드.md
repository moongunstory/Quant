# 알파 심사 가이드 (2026-07-24)

새 후보 알파를 라이브 편입까지 보내는 표준 절차. 후보 5개가 `data/strategy/alphas/`에 추가되어 있다:
`oi_conviction_momentum`(OI확인 모멘텀), `oi_crowding_fade`(레버리지 과적 fade), `low_vol_premium`(저변동성 프리미엄), `capitulation_reversal`(투매 반등), `avg_trade_size_follow`(큰손 체결 추종).
후보는 폴더에 있어도 config에 없으면 라이브에 절대 안 들어간다(안전).

## 0단계 — metrics 다운샘플 (1회만, 심사 전에)

R2 무료한도(10GB) 확보용. 기존 5분봉 6.4GB → 약 0.5GB.

```
python cli.py downsample-metrics --verbose
python -m src.live.remote_store push        # R2 사용 시 (로컬 전용이면 생략)
```

이후 수집은 자동으로 1시간봉으로 저장된다(코드 반영됨). 일 단위 패널 값은 보존됨(last 필드 오차 0, mean 필드 오차 <3%·전체 일수의 ~1%만).

## 1단계 — 일괄 스크리닝 (5분)

```
python cli.py backtest-rank --only oi_conviction_momentum oi_crowding_fade low_vol_premium capitulation_reversal avg_trade_size_follow
```

컷: `sharpe_hl`(반감기 샤프) ≤ 0 이거나 fitness < 0 이면 탈락. 여기서 절반쯤 떨어지는 게 정상.

## 2단계 — 개별 정밀 검증 (통과자만)

```
python cli.py backtest-test --alpha <이름> --validate
```

통과 기준(전부 만족):
- 순열검정 p-value < 0.05 (운이 아님)
- OOS 비율(oos/is) ≥ 0.5 (과거 절반에만 맞춘 게 아님)
- walkforward 폴드 양수비율 ≥ 50%
- turnover 확인: 높으면(>0.5/일) 비용 차감 후에도 남는지 ann_return으로 재확인

## 3단계 — 독립성 검사 (핵심)

```
python cli.py backtest-corr --only <통과한 후보들> taker_flow_follow range_high_60d crowd_ls_fade funding_carry_7d reversal_5d whale_concentration_fade smart_dumb_divergence
```

기존 채택 4개와 |corr| ≥ 0.5 → 탈락(같은 베팅의 복제).
특히 확인할 쌍: `capitulation_reversal`↔`reversal_5d`, `avg_trade_size_follow`↔`whale_concentration_fade`, `oi_crowding_fade`↔`crowd_ls_fade`.

## 4단계 — 포트폴리오 리허설 (config 복사본으로)

```
copy data\strategy\portfolio\config.json data\strategy\portfolio\config_test.json
```

`config_test.json`의 `alphas` 리스트에 생존자 추가 후:

```
python cli.py portfolio --config data/strategy/portfolio/config_test.json
python cli.py walkforward --config data/strategy/portfolio/config_test.json --save-report
```

기존 config 대비 walkforward OOS 샤프가 **좋아져야** 편입. 같거나 나쁘면 벤치에 두지 말 것(운영 부담만 늘어남).

## 5단계 — 편입 + 배포

1. 본 `config.json`의 `alphas`에 이름 추가 (selection의 recency 게이트가 이후 벤치/복귀를 자동 관리)
2. 새 패밀리면 `data/strategy/meta/alpha_families.json`에도 등록 (family 캡 적용용)
3. 재배포: `.\deploy.ps1` (알파 json + config는 Docker 이미지에 실려 감)

## 원칙 (요약)

- 한 번에 5~10개만, 전부 가설("누가 왜 이 돈을 주는가") 있는 것만. 대량 생산 금지 — 우연히 과거에 맞은 가짜가 심사를 통과한다.
- 백테스트 결과가 음수라고 부호를 뒤집어 재시도하지 않는다(사후 부호선택 = 과최적화). 예외는 폴드 전체가 일관되게 강한 음수일 때뿐.
- 결과가 너무 좋으면(샤프 3+) 기뻐하지 말고 미래참조부터 의심.
- 채택 알파가 쓰는 필드가 기존 수집 범위(klines/fundingRate/premiumIndexKlines/metrics) 안이면 Lambda 부담 증가는 계산 몇 분뿐. 새 데이터셋(bookDepth 등)을 쓰는 알파는 수집 비용을 따로 계산할 것.
